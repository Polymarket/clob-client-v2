PK=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Optional: second wallet used as counterparty for market order e2e tests (steps 7-9)
PK2=
CHAIN_ID=
CLOB_API_URL=

# Required for e2eOrderTypes.ts
TOKEN_ID=
# Optional: order size in USDC (defaults to market's minimum_order_size)
ORDER_SIZE=
# Required for PMCT wrapping and on-chain reads
RPC_URL=
# Required for conditional PMCT wrapping
COLLATERAL_TOKEN_ADDRESS=
WRAPPER_ADDRESS=
# Optional: set to a bytes32 builder code to run builder code order tests
BUILDER_CODE=